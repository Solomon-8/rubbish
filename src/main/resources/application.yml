spring:
  redis:
    host: 127.0.0.1
    port: 6379
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/exercise?useSSL=false&characterEncoding=utf8
    username: root
    password:
server:
  port: 8888
mybatis:
  type-aliases-package: com.exercise.exercise
  mapper-locations: classpath:mapper.com.exercise.exercise.mapper/*.xml
